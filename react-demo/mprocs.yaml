procs:
  client:
    cwd: packages/client
    shell: pnpm run dev
#  contracts:
#    cwd: packages/contracts
#    shell: pnpm mud deploy
#  anvil:
#    cwd: packages/contracts
#    shell: anvil --base-fee 0 --block-time 2
  indexer:
    cwd: packages/contracts
    shell: shx rm -rf $SQLITE_FILENAME && pnpm sqlite-indexer
    env:
      DEBUG: mud:*
      RPC_HTTP_URL: "https://snake-demo.adventurelayer.xyz/shard"
      FOLLOW_BLOCK_TAG: "latest"
      SQLITE_FILENAME: "indexer.db"
#  explorer:
#    cwd: packages/contracts
#    shell: pnpm explorer
